# ssh-spring-boot-starter

## 简介
### 1、封装了sshj，支持3种ssh验证方式
#### a、用户名/密码验证
#### b、用户名/密钥验证
#### c、用户名/密钥路径验证

### 2、配置示例
```text
ssh.auth-method=user_password
ssh.host=192.168.146.130
ssh.login-name=root
ssh.password=123456
```
